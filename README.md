# Ormoc Transport App 

*Install Libraries, Dependencies, and Tools first before getting started with the project.*
*A full summary of the installed tools is listed inside the `requirements.txt` file, along with it's specific versions to avoid incompatibility. Install accordingly.*


## Pre-requisities

Assume you already have and activated your own *virtual environment*, and installed all requirements (read `requirements.txt` file from the root directory of the project), you can now follow these next steps and guides accordingly, this serves as a guide on:
  - *what the system does*; 
  - *what and who it's for*; 
  - *what are the technologies used*;
  - *what are the main functionalities of the system*


Use *lucide-react@1.38.0* version when installing lucide react icons for compatibility


